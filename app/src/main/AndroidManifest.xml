<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <application
        android:name=".Application"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.VerixSettings">
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:resource="@string/description" />
        <meta-data
            android:name="xposedminversion"
            android:value="93" />
        <meta-data
            android:name="xposedsharedprefs"
            android:value="true" />
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposedscope" />

        <provider
            android:name=".provider.SharedPrefsProvider"
            android:authorities="com.xlp.verixsettings.provider.sharedprefs"
            android:directBootAware="true"
            android:exported="true"
            android:grantUriPermissions="true"
            tools:ignore="ExportedContentProvider" />

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="de.robv.android.xposed.category.MODULE_SETTINGS" />
            </intent-filter>
        </activity>

        <activity-alias
            android:name=".ui.MainActivityAlias"
            android:exported="true"
            android:label="@string/app_name"
            android:targetActivity=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>


        <activity
            android:name=".ui.pages.UiActivity"
            android:label="@string/Ui" />
        <activity
            android:name=".ui.pages.VibratorActivity"
            android:label="@string/Vibrator" />
        <activity
            android:name=".ui.pages.ChargeActivity"
            android:label="@string/Charge" />
        <activity
            android:name=".ui.pages.FingerPrintActivity"
            android:label="@string/Fingerprint" />
        <activity
            android:name=".ui.pages.OtherActivity"
            android:label="@string/Other" />
        <activity
            android:name=".ui.pages.PackageActivity"
            android:label="@string/Package" />
    </application>
</manifest>